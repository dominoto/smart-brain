{
  "name": "facerecognitionbrain",
  "version": "0.1.0",
  "engines": {
    "node": "17.x",
    "npm": "8.5.x"
  },
  "private": true,
  "dependencies": {
    "clarifai": "^2.9.1",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-particles-js": "^3.6.0",
    "react-scripts": "^4.0.3",
    "react-tilt": "^0.1.4",
    "serve": "^14.0.1",
    "tachyons": "^4.12.0"
  },
  "scripts": {
    "start": "serve -s build --openssl-legacy-provider start",
    "build": "react-scripts build --openssl-legacy-provider start",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm install --force && npm run build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}